{
  "name": "tidemark-extension",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc && node build.js",
    "watch": "tsc --watch",
    "validate": "node validate.js"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.278",
    "typescript": "^5.7.3"
  }
}
